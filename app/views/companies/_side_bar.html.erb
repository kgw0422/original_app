<div class="side-bar-header">
  <div class="header-name">
  </div>
  <div class="create-room">
    <%= link_to "ユーザー名","#" %>
  </div>
</div>

<div class="rooms">
  <% @info.each do |company| %>
    <div class="room">
      <div class="room-name">
         <%= link_to company.company_name, company_path(company.id) %>
      </div>
    </div>
  <% end %>
</div>
